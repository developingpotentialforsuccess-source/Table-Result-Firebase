{
  "entities": {
    "Level": {
      "title": "Level",
      "description": "Grading structure for a specific level.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "subjects": { "type": "array" },
        "gradingScale": { "type": "array" }
      },
      "required": ["id", "name", "subjects"]
    },
    "ClassRecord": {
      "title": "Class Record",
      "description": "A class record metadata (without students).",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "termName": { "type": "string" },
        "className": { "type": "string" },
        "teacherName": { "type": "string" },
        "levelId": { "type": "string" },
        "isPinned": { "type": "boolean" },
        "createdAt": { "type": "string" },
        "updatedAt": { "type": "string" }
      },
      "required": ["id", "termName", "className", "teacherName", "levelId"]
    },
    "Student": {
      "title": "Student",
      "description": "Student record with grades and attendance.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "sex": { "enum": ["Male", "Female"] },
        "scores": { "type": "object" },
        "attendance": { "type": "string" },
        "comment": { "type": "string" }
      },
      "required": ["id", "name", "sex", "scores"]
    }
  },
  "firestore": {
    "/levels/{levelId}": {
      "schema": { "$ref": "#/entities/Level" },
      "description": "Customized levels shared across all users"
    },
    "/classes/{classId}": {
      "schema": { "$ref": "#/entities/ClassRecord" },
      "description": "Class records (metadata) shared across all users"
    },
    "/classes/{classId}/students/{studentId}": {
      "schema": { "$ref": "#/entities/Student" },
      "description": "Students within a class shared across all users"
    }
  }
}
